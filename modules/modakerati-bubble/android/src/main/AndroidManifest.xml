<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- Required to draw the floating chat head over other apps (Messenger-style).
       Merged into the app manifest by the Android manifest merger. -->
  <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
</manifest>
